% ============================================================
% Appendix — lean v15 version.
% Only the formal results, supporting tables, and figures
% strictly required by the main text.
% Cuts from v13: bid-level structural mixture, Bajari-Ye tests,
% network analyses, market-selection / Regime 1 / Regime 2 cover-bid
% distribution propositions, welfare illustrative calculations,
% cross-sector replication, dyadic linkage, Cox survival, network-
% quadrant heterogeneity, FL characteristics — none of which v15
% main body references.
% ============================================================

\section{Cover-Bidding Framework and Proofs}\label{app:proofs}

This appendix formalizes the separating-equilibrium framework
referenced throughout the main text. Three results are derived
and used: a lemma establishing the bright-line filter
$\text{wins}=0$ as the equilibrium choice of the cover-bidder
type (Lemma~\ref{lem:wins_zero_stmt}); a proposition identifying
$\log(1+\text{tenders\_count})$ as a sufficient ranking statistic
for the cover-bidder type given award-record data
(Proposition~\ref{prop:exposure_stmt}); and a proposition
characterizing the comparative static of optimal cover-bidder
deployment in the procuring unit's principal cost of detection
(Proposition~\ref{prop:optimal_m}). The framework is an
organizing device for the empirical patterns; it is not the
source of identification.

\paragraph{Setup}
A cartel of $n$ firms in a procurement market controls a
designated winner with bid $b^*$ and deploys $m \geq 0$ cover
bidders, each submitting a bid $b_\ell > b^*$. Per-bidder
deployment cost is $c_1 > 0$; the cartel faces a detection
probability $\theta_k \in (0,1)$ that depends on oversight
capacity in market $k$ (smaller, less-supervised buyers have
lower $\theta_k$). The cartel maximizes expected surplus
$\pi(m, \theta_k, c_1)$, the difference between cartel rents
and the sum of cover-bidder costs and expected detection
penalties. We impose two standard assumptions:

\begin{description}
\item[A1 (Profit positivity).] $\partial \pi / \partial m \big|_{m=0} > 0$:
the first cover bidder generates positive marginal cartel
surplus, otherwise no cartel deploys cover.
\item[A2 (Diminishing returns).] $\partial^2 \pi / \partial m^2 < 0$:
the surplus function is strictly concave in $m$, so an interior
optimum exists.
\end{description}

The empirical environment is described by award-record data:
each firm $i$ has a participation count $\text{tenders\_count}_i$,
a win indicator $\text{wins}_i \in \{0, 1\}$, and a tenure $\tau_i$.
The construct flags firms with $\text{wins}_i = 0$ and
$\text{tenders\_count}_i$ above the operational threshold
$\valThreshold$. Lemma~\ref{lem:wins_zero_stmt} below formalizes
the role of the wins-zero condition;
Proposition~\ref{prop:exposure_stmt} formalizes the role of the
participation-count ranking.

\begin{lemma}[Wins-zero separating equilibrium]\label{lem:wins_zero_stmt}
Suppose cover bidders draw their bid $b_C$ from a strategy that
satisfies (a) $b_C > b^*$ in equilibrium (cartel-allocation
constraint) and (b) winning triggers either capacity revelation
(undermining the cover identity in subsequent rounds) or internal
cartel sanctions on the deviating firm. Genuine entrants of type
$G$ face no such constraint and accept any $b_G < b^*$ that
yields positive expected payoff. Then in the separating
equilibrium, cover bidders satisfy
$\Pr(\text{win} \mid C) = 0$: the bright-line filter
$\text{wins}=0$ is the equilibrium choice of type $C$.
\end{lemma}

\begin{proof}[Proof of Lemma~\ref{lem:wins_zero_stmt}]
Write the cover-bidder per-period payoff as
$u_C(b) = -c_1 - \theta_k \phi_0 \cdot \mathbf{1}[\text{win}]
       - \kappa \cdot \mathbf{1}[\text{win}]$, where $\phi_0 > 0$
is the marginal expected detection penalty per cover bid and
$\kappa > 0$ captures expected cartel-internal sanction or
capacity-revelation cost. Choosing $b_C > b^*$ delivers
$\Pr(\text{win} \mid b_C, b^*) = 0$ in any equilibrium where the
designated winner submits $b^*$, so $u_C = -c_1$. Any deviation
to $b_C' < b^*$ delivers
$u_C' = -c_1 - \theta_k \phi_0 - \kappa + \mathrm{rents}$, where
$\mathrm{rents}$ is the deviation gain from winning. Under the
cartel's allocation rule, $\mathrm{rents} < \kappa$ for any cover
bidder (otherwise the allocation would not be
incentive-compatible). Hence $u_C > u_C'$, and the equilibrium
strategy is $b_C > b^*$ with $\Pr(\text{win} \mid C) = 0$. By
contrast, type $G$ has no $\kappa$ penalty and accepts $b_G < b^*$
when expected payoff is positive. Hence the empirical filter
$\text{wins} = 0$ identifies the type-$C$ subset, conditional on
the cartel deploying its allocation rule. \qed
\end{proof}

\begin{proposition}[Linearized cumulative exposure]\label{prop:exposure_stmt}
Suppose firm $i$ is a cover bidder of type $C$ deployed by a
cartel in market $k$ at per-period rate
$\lambda_C(\theta_k, c_1)$ and tenure $\tau_i$. Under A1--A2 and a
stationary deployment process,
\[
  \mathbb{E}\!\left[\,\text{tenders\_count}_i \mid
                   \text{type}=C,\,\tau_i\right]
  \;=\; \lambda_C(\theta_k, c_1)\,\tau_i.
\]
For genuine competitive entrants of type $G$,
$\mathbb{E}[\text{tenders\_count}_i \mid \text{type}=G, \tau_i]
= \lambda_G\,\tau_i$ with $\lambda_G < \lambda_C$ in any
cartel-targeted market: by Proposition~\ref{prop:optimal_m}
below, $m^* > 0$ implies positive equilibrium deployment of
cover bidders, while genuine entry does not respond to cartel
demand for participation slots. The log-statistic
$\log(1+\text{tenders\_count}_i)$ is therefore monotone in
$\lambda$ given tenure, and---under monotone likelihood ratio in
the Poisson approximation
$T_i \sim \mathrm{Poisson}(\lambda \tau_i)$---the posterior
$\Pr(\text{type}=C \mid \text{tenders\_count}_i = t)$ is
non-decreasing in $t$. Combined with
Lemma~\ref{lem:wins_zero_stmt}, the joint statistic
$(\log(1+\text{tenders\_count}),\,\mathbf{1}[\text{wins}=0])$ is
a sufficient screen for the cover-bidder type given award-record
data; the deployable binary frequent-loser rule is the
information-coarsening of this latent posterior under the
separating equilibrium.
\end{proposition}

\begin{proof}[Proof of Proposition~\ref{prop:exposure_stmt}]
The expectation result is immediate from the law of large
numbers applied to the deployment counting process given
constant rate $\lambda$. The monotonicity claim follows from
Bayes' rule:
\[
  \Pr(C \mid t) =
  \frac{\Pr(t \mid C)\,\pi_C}
       {\Pr(t \mid C)\,\pi_C + \Pr(t \mid G)\,(1-\pi_C)},
\]
which is non-decreasing in $t$ if and only if the likelihood
ratio $\Pr(t \mid C)/\Pr(t \mid G)$ is non-decreasing in $t$.
For Poisson likelihoods with rates $\lambda_C > \lambda_G$, this
monotone-likelihood-ratio property is standard. Hence
$\log(1+\text{tenders\_count})$ is a sufficient ranking statistic
for the cover-bidder type, and the binary indicator
$\mathbf{1}[\text{tenders\_count} > k]$ is its
information-coarsening operational form. \qed
\end{proof}

\begin{proposition}[Optimal cover-bidder deployment]\label{prop:optimal_m}
Under A1--A2, the cartel's optimal cover-bidder deployment rate
$m^*$ satisfies the first-order condition
$\partial \pi / \partial m = c_1 + \theta_k \phi_0$, where
$\phi_0 > 0$ is the marginal expected detection penalty per
cover bidder evaluated at the optimum. The implicit comparative
statics are
\[
  \frac{\partial m^*}{\partial \theta_k}
  = -\,\frac{\phi_0}{\partial^2 \pi / \partial m^2} \;<\; 0,
  \qquad
  \frac{\partial m^*}{\partial c_1}
  = -\,\frac{1}{\partial^2 \pi / \partial m^2} \;<\; 0.
\]
\end{proposition}

\begin{proof}[Proof of Proposition~\ref{prop:optimal_m}]
The first-order condition follows directly from differentiating
$\pi(m, \theta_k, c_1)$ with respect to $m$ and equating the
marginal cartel surplus to the marginal cost of the cover bidder
($c_1$ deployment cost plus expected detection penalty
$\theta_k \phi_0$). By A2, $\partial^2 \pi / \partial m^2 < 0$,
so the implicit interior solution $\tilde m$ defined by the FOC
is unique. Implicit differentiation of the FOC yields the
comparative statics displayed, both negative because the
denominator is negative. The empirical implication is that
cover-bidder deployment is more aggressive where the procuring
unit's principal cost of detection is lower
(§\ref{sec:results_oversight}). \qed
\end{proof}

% ──────────────────────────────────────────────────────────────────
\section{Core Supporting Tables and Figures}
\label{app:supporting}
% ──────────────────────────────────────────────────────────────────

\input{./output/tables/tab_modality_by_year}

\input{./tables/tab_conditional_descstats}

\input{./output/tables/tab_regime_oversight}

\input{./output/tables/tab_falsification_modal}

\input{./tables/tab_mccrary}

\input{./output/tables/tab_excl_cade}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.75\textwidth]{fig_01_losses_distribution}
\caption{Distribution of tender-item participation counts across
always-loser firms in BEC, $\valYearStart$--$\valYearEnd$. The
distribution is heavily right-skewed; a small share of always-losers
participates in tens or hundreds of tender-items without ever
winning.}
\label{fig:losses}
\end{figure}

\begin{figure}[htbp]
\centering
\includegraphics[width=0.75\textwidth]{fig_02_iqr_identification}
\caption{Identification of the frequent-loser threshold via
median + $1.5 \times$ IQR on the always-loser participation
distribution. The cutoff yields $\valThreshold$ tender-items
($\valThresholdStat$ statistical threshold) and flags $\valFL$
firms.}
\label{fig:iqr}
\end{figure}

% ──────────────────────────────────────────────────────────────────
\section{Identification Audits}
\label{app:online_robustness}
% ──────────────────────────────────────────────────────────────────

\input{./output/tables/tab_oster_delta}

\input{./output/tables/tab_leakage_audit}

% ──────────────────────────────────────────────────────────────────
\section{Threshold and Specification Robustness}
\label{app:robustness_tables}
% ──────────────────────────────────────────────────────────────────

\begin{table}[htbp]
\centering
\caption{Price Coefficient Across IQR Multiplier Thresholds}
\label{tab:threshold}
\input{./output/tables/tab_threshold_robustness}
\end{table}

\begin{table}[htbp]
\centering
\caption{Alternative Clustering of Standard Errors}
\label{tab:clustering}
\input{./output/tables/tab_clustering_robustness}
\end{table}

% ──────────────────────────────────────────────────────────────────
\section{Operational Metrics and Detector Comparison}
\label{app:operational}
% ──────────────────────────────────────────────────────────────────

\input{./output/tables/tab_operational_metrics}

\input{./output/tables/tab_imhof_incremental}

% ──────────────────────────────────────────────────────────────────
\section{Adversarial-Adaptation Simulation}
\label{app:adversarial}
% ──────────────────────────────────────────────────────────────────

A natural concern is that a sophisticated cartel could adapt its
deployment to evade the construct. We examine four strategic
adaptations that hold the construct fixed and modify the
always-loser pool to mimic plausible evasive behavior, then
recompute the firm-level AUC of $\log(1+\text{tenders\_count})$
against the $193$ CADE-cobidder labels.
Table~\ref{tab:adversarial_adaptation} reports the degradation
profile.

\input{./output/tables/tab_adversarial_adaptation}

The construct is resilient to two adaptations and vulnerable to
two others. Rotation ($20\%$ of cover bidders retire annually and
are replaced by fresh CNPJs) and occasional wins ($5\%$ of
threshold-crossing firms win one tender each) leave AUC essentially
unchanged: the bright-line filter eliminates the small share of
newly-winning cover bidders, and rotation reshuffles the
always-loser pool without compressing the ranking statistic. CNPJ
splitting (each frequent loser splits into two CNPJs, each carrying
half the participation count) degrades AUC by roughly $4$ points;
threshold-aware capping (firms cap participation at $13$
tender-items to stay below the median + $1.5\!\times$IQR cutoff)
degrades AUC by roughly $13$ points. The combined attack
(threshold capping with $20\%$ rotation) drives AUC from $0.928$
to $0.632$, a $30$-point compression.

The simulation has two implications for deployment. First, the
construct is not a black box: a strategic actor with knowledge of
the threshold rule can degrade the screen substantially, and the
threshold rule is the most exposed margin. Second, the construct's
operational lifetime in any given jurisdiction depends on whether
strategic adaptation is observable and whether the threshold can be
periodically refreshed. We do not claim the construct is
adaptation-proof; we claim it is informative absent adaptation and
degrades predictably under specified attacks. Oversight bodies
deploying the screen should plan for periodic recalibration and
should integrate the construct with bid-distribution methods where
those data are available, so that combined detection survives
adaptations against either component alone.

% ──────────────────────────────────────────────────────────────────
\section{Staggered Designs Attempted and Rejected}
\label{app:staggered}
% ──────────────────────────────────────────────────────────────────

The difference-in-differences design exploiting Decreto~$\valDecreto$'s
$2018$ raise of the convite cap fails the parallel-trends
diagnostic at the available pre-treatment window. We report two
attempted estimators alongside the failure mode. The
\citet{sun2021estimating} interaction-weighted estimator yields
imprecise coefficients with non-monotone dynamic patterns
incompatible with the smooth treatment-onset narrative the cap
raise would imply (Table~\ref{tab:did_revised}). A stacked
difference-in-differences design constructed around the cap-raise
event window returns coefficients that flip sign across stacks
and lack a clean pre-period (Table~\ref{tab:stacked_did}). We
treat both estimators as null at the available bandwidths and
report the empirical content of §\ref{sec:results} as
descriptive throughout (§\ref{sec:lim_identification}).

\input{./output/tables/tab_did_revised}

\input{./output/tables/tab_stacked_did}
