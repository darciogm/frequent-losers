% DRAFT SALVAGE BLOCK — closing subsection for Appendix G (Federal Audit Battery).
% ============================================================================
% Provenance: distilled from the now-DELETED orphan sec_app07_comprasnet_submission.tex,
% re-pitched at the v22 canonical altitude (the deliverable is the AUDIT, not a degraded-
% but-positive ranking). Only TWO slivers survive the gut: (1) the per-stage graduation
% view ("what ports / what deflates / what boundary holds" federally vs. BEC), and (2) the
% structural-boundary statement (the score does not rank defendants/winners). One linkage-
% provenance sentence is also carried, stripped of the falsified "independent universe" framing.
%
% INTEGRATION: paste as the penultimate subsection of sec_appG_federal_DRAFT.tex, immediately
% BEFORE "\subsection{Replication}". DO NOT \input this file anywhere. DO NOT edit values.tex.
% Every numeric claim is a \valTODO placeholder with an inline % TODO_NUMBER tag naming the
% federal \valFedAud* macro the lead wires after the run chain. NEW macros introduced here:
% \valFedAudBoundaryAUC, \valFedAudBoundaryAUCLow, \valFedAudBoundaryAUCHi,
% \valFedAudDirectsWinshare, \valFedAudDirectsN.
%
% FALSIFIED CONTENT DELIBERATELY ABSENT (Phase 0): no 2009-2019 window, no convite, no
% "independent cartel universe", no item-stem/item-group cells, no winner_vs_ref price
% mechanism, no raw-cobidder-AUC "survives above chance" headline, no v20 artifact paths.
% ----------------------------------------------------------------------------
\providecommand{\valTODO}{\textbf{[??]}}
% ============================================================================

\subsection{Graduation: What Ports and What Deflates}
\label{app:federal_graduation}

This closing subsection reads the federal battery at claim altitude:
for each stage of the audit, what \emph{ports} from BEC, what
\emph{deflates} under federal discipline, and where the structural
boundary holds on both platforms. The federal CADE anchors are the
same numbered adjudicated processes used on BEC, recovered through a
multi-pass establishment-anchored linkage (BEC-side crossmatch reuse,
then RAIS employment-link audit, then a Receita Federal \emph{Empresas}
open-dump pass), and are \emph{partially overlapping} with the BEC
anchors by construction (\ref{sec:comparative}); the table below reports
portability of forensic priority, not an independent cartel population.

\begin{table}[htbp]
\centering
\caption{Federal Graduation: Audit-Stage Verdicts, BEC--SP versus ComprasNet}
\label{tab:federal_graduation}
\scriptsize
\begin{adjustbox}{max width=\textwidth,center}
\begin{threeparttable}
\begin{tabular}{p{5.0cm}ccp{3.4cm}}
\toprule
Audit stage & BEC--SP & ComprasNet & Federal verdict \\
\midrule
Raw award-layer concentration (ROC-AUC) & $\valTODO$ & $\valTODO$ & Ports (pre-adjustment only) \\ % TODO_NUMBER \valExpRawScoreAUC | \valFedAudRawAUC
Label-blind opportunity expectation (AUC) & $\valTODO$ & $\valTODO$ & Concentration $\approx$ opportunity \\ % TODO_NUMBER \valArmorExpLB | \valFedAudLabelBlindAUC
Within-stratum residual (AUC, MEDIUM cells) & $\valTODO$ & $\valTODO$ & $\valTODO$ \\ % TODO_NUMBER \valExpWithinAUC | \valFedAudWithinAUC | verdict: deflates / survives (set after run chain)
Matched-permutation $p$ (within-strata shuffle) & $\valTODO$ & $\valTODO$ & $\valTODO$ \\ % TODO_NUMBER \valExpPermCp | \valFedAudPermCp | verdict mirrors the within-stratum row
Label-frozen prospective timing (AUC) & $\valTODO$ & $\valTODO$ & Prospective collapse ports \\ % TODO_NUMBER \valArmorFrozenProspAUC | \valFedAudFrozenProspAUC
Structural boundary: score vs.\ direct defendants (AUC) & $\valTODO$ & $\valTODO$ & Boundary holds (does not rank winners) \\ % TODO_NUMBER \valDirectDefAUC | \valFedAudBoundaryAUC
Negative-control verdict & $\valTODO$ & $\valTODO$ & $\valTODO$ \\ % TODO_NUMBER \valTODO(BEC: generic geometry) | \valFedAudNegControlVerdict | generic geometry / cartel-specific
\bottomrule
\end{tabular}
\begin{tablenotes}
\scriptsize
\item \textit{Notes:} Each row is the comparable quantity from the
federal battery (\ref{app:federal_control_function}--%
\ref{app:federal_negative_controls}) set beside its BEC analogue.
The raw concentration row is reported \emph{only} as the
pre-adjustment baseline; it is opportunity arithmetic once the
label-blind expectation is credited, and it is not a deployable
ranking on either platform. Cobidder labels are adjudication-anchored
exposure, not cartel membership; neither column ranks defendants. The
federal panel has no sealed-bid modality, so no modality
stratification is reported. Every cell is a placeholder pending the
federal run chain (\valTODO).
\end{tablenotes}
\end{threeparttable}
\end{adjustbox}
\end{table}

\paragraph{The structural boundary holds on both platforms.}
The one positive cross-platform statement the graduation supports is a
\emph{negative} about the screen's reach, and it is on-thesis. On the
federal panel the firm-level AUC of the loser-side score against
\emph{direct} CADE defendants is $\valTODO$ % TODO_NUMBER \valFedAudBoundaryAUC
($[\valTODO,\valTODO]$), at or below chance, exactly as on BEC. % TODO_NUMBER \valFedAudBoundaryAUCLow | \valFedAudBoundaryAUCHi
The reason is the same on both platforms: direct defendants in these
adjudications are predominantly winners
($\valTODO$ of $\valTODO$ federal direct-defendant rows are not % TODO_NUMBER \valFedAudDirectsWinshare | \valFedAudDirectsN
always-losers), and a loser-side construct by design does not rank
winners. The screen concentrates loser-side exposure and declines to
rank the very firms a naive reader might expect it to flag; that
boundary is a feature of the construct, and it ports.

\paragraph{Reading the graduation.}
The federal battery does not return a degraded-but-positive
deployable ranking, and the appendix does not claim one. What ports is
the \emph{audit}: the raw award-layer concentration looks informative
until opportunity exposure is held fixed, after which the within-stratum
residual is read directly off Table~\ref{tab:federal_graduation}, and
the prospective ranking behaves as it does on BEC. The deliverable an
agency carries across platforms is therefore the decomposition that
tells it how far a cheap award-layer statistic can be trusted before
costly bid-layer work begins---and on the federal system that lesson
binds harder, because the bid-layer step cannot be constructed from
public data at all (\ref{sec:comparative_observability}).
