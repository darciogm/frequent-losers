% DRAFT SKELETON — Phase 3 prep, numbers pending federal run chain (Phase 1/2). NOT \input anywhere.
% ============================================================================
% Standalone draft of proposed Appendix G "Federal Audit Battery (ComprasNet)".
% Mirrors the BEC validation-audit appendix (sec_app03_validation_audits_submission.tex):
% same threat-family subsection order (funnel -> opportunity cells / expected
% contact -> opportunity-adjusted -> timing/leakage -> case LOCO -> negative
% controls -> armor), same table style (scriptsize threeparttable in adjustbox),
% same cell-definition note (COARSE/MEDIUM/STRICT). This is a COMPANION to the
% existing sec_app07_comprasnet_submission.tex (which is the headline-replication
% "what survives / what fails" appendix); Appendix G is the full federal deflation
% battery that parallels the BEC audit, feeding the comparative section.
%
% Placement is decision D-iii (pending). DO NOT \input. DO NOT edit values.tex.
% Every numeric claim is a \valTODO placeholder with an inline % TODO_NUMBER tag
% naming the BEC analogue macro and the proposed federal macro for the lead to
% wire later. New federal-audit macros use the \valFedAud* namespace to avoid
% colliding with the existing \valFed* replication macros in sec_app07.
% ----------------------------------------------------------------------------
\providecommand{\valTODO}{\textbf{[??]}}
% ============================================================================

\section{Federal Audit Battery (ComprasNet)}
\label{app:federal_audit_submission}

This appendix re-runs the full BEC validation battery of
\ref{app:validation_audits_submission} on the federal ComprasNet
panel ($2013$--$2019$), stage for stage, under the same definitions.
It is the audit-side companion to the headline replication in
\ref{app:comprasnet_submission} and supplies the federal column of
Table~\ref{tab:comparative_audit}. Throughout, ``CADE-defendant
contact'' denotes an \emph{adjudication-anchored exposure} label: a
firm shares a tender-item with a directly adjudicated CADE
defendant. It does not assert cartel membership, agreement, or
liability. The federal panel has no sealed-bid modality, so no
modality stratification is reported; the legal anchors are the same
numbered CADE cases used on BEC, partially overlapping by
construction (\ref{sec:comparative}). The audits validate the
portability of forensic priority, not membership, damages, or proof.

\subsection{Label-Construction Funnel}
\label{app:federal_funnel}

\textit{Purpose.} Reconcile the federal validation denominators
before any score is credited, mirroring
Table~\ref{tab:label_funnel_submission}.

\textit{Construction.} Direct CADE defendants are matched to federal
participant records by the canonical establishment-anchored CNPJ
rule; their defendant tender-items locate the adjudicated
environments; unique always-loser firms sharing at least one
federal tender-item with an anchor define the broad cobidder label.
Direct defendants are excluded and the frequent-loser flag is never
used to build the label.

\begin{table}[htbp]
\centering
\caption{Federal Label-Construction Funnel}
\label{tab:federal_funnel}
\scriptsize
\begin{adjustbox}{max width=\textwidth,center}
\begin{threeparttable}
\begin{tabular}{p{3.6cm}cccccp{2.6cm}}
\toprule
Sample / benchmark & Cases & Legal def. & Fed-active def. & AL cobid. & Broad-AL cobid. & Role \\
\midrule
Numbered CADE portfolio & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ & reference \\ % TODO_NUMBER \valFedAudCases / \valFedAudLegalDef / \valFedAudFedActiveDef / \valFedAudCobiddersAll / \valFedAudCobiddersBroadAL
Main federal target & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ & primary \\ % TODO_NUMBER mirror row
Conservative (pre-cutoff) & $\valTODO$ & --- & $\valTODO$ & $\valTODO$ & $\valTODO$ & early-cases subset \\ % TODO_NUMBER \valFedAudConsCases / \valFedAudConsFedDef / \valFedAudConsCobidders / \valFedAudConsBroadAL
\bottomrule
\end{tabular}
\begin{tablenotes}
\scriptsize
\item \textit{Notes:} A cobidder is an adjudication-anchored
exposure label, not membership. The main label is a unique
always-loser firm (federal win rate $=0$ over $2013$--$2019$)
sharing at least one federal tender-item with a federally active
direct CADE defendant; direct defendants are excluded; the
frequent-loser flag is not used to construct the label. One
unnumbered summary case is excluded for lack of firm-level
identification. Counts are unique firms.
\end{tablenotes}
\end{threeparttable}
\end{adjustbox}
\end{table}

\subsection{Opportunity-Cell Construction and Expected Contact}
\label{app:federal_opp_cells}

\textit{Purpose.} A firm can appear near a CADE anchor simply
because it operates in environments where defendants are active; the
opportunity cells discipline this exposure before any score is
credited, exactly as in \ref{app:opp_cells_submission}.

\textit{Method.} We partition the federal award layer into
procurement-opportunity cells $g$ using three nested definitions,
adapted to federal observability:
\textbf{COARSE} $=$ year (the item-group margin is not observed
federally---federal item codes are buyer-embedded rather than drawn
from a shared classification, \ref{sec:comparative_observability});
\textbf{MEDIUM} $=$ year $\times$ buyer (UASG)---the reference
definition for the headline federal within-stratum results; and
\textbf{STRICT} $=$ item code $\times$ year $\times$ buyer. Modality
is not a margin (the federal panel is pure Preg\~ao). For firm $i$,
with $n_{ig}$ the tender-items held in cell $g$ and $p_g$ the cell
defendant-contact rate computed leave-one-out, expected contact is
$E_i=\sum_g n_{ig}\,p_g$, observed contact is $O_i$, and excess is
$X_i=O_i-E_i$.

\textit{Result (pending).} The mean cell defendant-contact rate
$\bar p_g$ is around $\valTODO$ across the three definitions; finer % TODO_NUMBER \valFedAudPbarRange (BEC: 0.017--0.019)
cells gain resolution at the cost of support (under STRICT, $\valTODO$ % TODO_NUMBER \valFedAudStrictSparseShare (BEC: 99.5%)
of cells hold fewer than five tender-items). Common-support
retention under MEDIUM cells is about $\valTODO$ of firms, and under % TODO_NUMBER \valFedAudMediumRetention (BEC: 46%)
STRICT about $\valTODO$. The cell-construction and retention grids % TODO_NUMBER \valFedAudStrictRetention (BEC: 9%)
are in the federal supplement.

\subsection{Opportunity-Adjusted Validation}
\label{app:federal_control_function}

\textit{Purpose.} Ask whether the federal screening score still
ranks defendant contact once opportunity exposure $E_i$ is in the
model, mirroring Table~\ref{tab:control_function_submission}.

\begin{table}[htbp]
\centering
\caption{Federal Control-Function Validation (Selected Specifications)}
\label{tab:federal_control_function}
\scriptsize
\begin{adjustbox}{max width=\textwidth,center}
\begin{threeparttable}
\begin{tabular}{lp{4.0cm}rrrr}
\toprule
Spec & Model & $N$ & Pos. & ROC-AUC & PR-AUC \\
\midrule
S0 & raw score ($\log T$) & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ \\ % TODO_NUMBER \valFedAudRawN / \valFedAudCobiddersBroadAL / \valFedAudRawAUC / \valFedAudRawPRAUC
S0 & raw FL (binary cut) & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ \\ % TODO_NUMBER \valFedAudRawFLAUC / \valFedAudRawFLPRAUC
S0 & exposure-only (all AL) & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ \\ % TODO_NUMBER \valFedAudExpOnlyAUC / \valFedAudExpOnlyPRAUC
S2 & score $+\log(1{+}E)$ [MEDIUM] & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ \\ % TODO_NUMBER \valFedAudCombinedAUC (label-encoding; not score evidence)
NESTED & exposure-only logit & $\valTODO$ & $\valTODO$ & $\valTODO$ & --- \\ % TODO_NUMBER \valFedAudExposedN / \valFedAudNestedExpAUC
NESTED & exposure $+$ score & $\valTODO$ & $\valTODO$ & $\valTODO$ & --- \\ % TODO_NUMBER \valFedAudNestedExpScoreAUC (increment \valFedAudNestedIncrement, DeLong p \valFedAudNestedP)
WITHIN & score $\mid$ opportunity stratum & $\valTODO$ & $\valTODO$ & $\valTODO$ & --- \\ % TODO_NUMBER \valFedAudWithinAUC
WITHIN & FL $\mid$ opportunity stratum & $\valTODO$ & $\valTODO$ & $\valTODO$ & --- \\ % TODO_NUMBER \valFedAudWithinFLAUC
LABEL-BLIND & opportunity-only (non-cobidder $p_g$) & $\valTODO$ & $\valTODO$ & $\valTODO$ & --- \\ % TODO_NUMBER \valFedAudLabelBlindAUC
\bottomrule
\end{tabular}
\begin{tablenotes}
\scriptsize
\item \textit{Notes:} Outcome is adjudication-anchored cobidder
contact. As on BEC, the combined exposure-plus-score logit (S2) is
mechanically inflated because $E_i$ is built from the same
defendant-contact rates that define the label; it is \emph{not}
score evidence. The score's isolated contribution is the nested
increment over the exposure-only model and the within-stratum AUC.
The LABEL-BLIND row recomputes each cell's defendant density from
non-cobidder rows only, so no eventual positive contributes to any
firm's expected contact. Every cell is a placeholder pending the
federal run chain (\valTODO).
\end{tablenotes}
\end{threeparttable}
\end{adjustbox}
\end{table}

\textit{Result (pending).} The federal reading is reported in
Table~\ref{tab:federal_control_function} and summarized in the
comparative verdict (Table~\ref{tab:comparative_audit}). The nested
DeLong increment over the exposure-only model is $\valTODO$ % TODO_NUMBER \valFedAudNestedIncrement (BEC: +0.010)
($p=\valTODO$), and the within-opportunity-stratum AUC for the score % TODO_NUMBER \valFedAudNestedP
is $\valTODO$. The label-blind opportunity ranking retains $\valTODO$ % TODO_NUMBER \valFedAudWithinAUC | \valFedAudLabelBlindAUC
on its own.

\subsection{Power Bounding and Matched Permutation}
\label{app:federal_power_permutation}

\textit{Purpose.} Establish that a within-stratum null is
informative rather than underpowered, and that the matched-strata
permutation is correctly sized, mirroring the BEC armor pack.

\textit{Method.} A positive-control score ($O_i$ itself) ranks the
label within the \emph{same} strata to confirm the design detects a
residual when one exists; a calibration injects synthetic
within-stratum signal to size the permutation test; and the label is
shuffled only within matched exposure-by-participation strata.

\textit{Result (pending).} The within-stratum positive control
reaches $\valTODO$, confirming detection power. The permutation test % TODO_NUMBER \valFedAudOiControlMedium (BEC: positive-control AUC)
is correctly sized under the null and rejects with probability
$\valTODO$ once the true within-stratum AUC reaches $0.55$ % TODO_NUMBER \valFedAudPowerFive (BEC: \valArmorPowerFive)
(probability $\valTODO$ at $0.60$). Under the matched-strata % TODO_NUMBER \valFedAudPowerTen
permutation the observed PR-AUC does $\valTODO$ reject % TODO_NUMBER \valFedAudPermVerdict (does / does not)
($p=\valTODO$); the within-stratum AUC across cell granularities % TODO_NUMBER \valFedAudPermCp
ranges $\valTODO$ (coarse) to $\valTODO$ (strict). % TODO_NUMBER \valFedAudWithinCoarse | \valFedAudWithinStrict

\subsection{Timing, Leakage, and Label-Frozen Prospective Test}
\label{app:federal_timing}

\textit{Purpose.} Ask whether the raw federal ranking can be used
prospectively, mirroring Table~\ref{tab:timing_case_holdout}.

\textit{Method.} We freeze the score, the always-loser status, and
the frequent-loser threshold to the $2013$--$2016$ window and
evaluate against $2017$--$2019$ labels, on the realistic full
candidate universe (entrants carried at score zero). A fully
label-frozen variant additionally freezes the label
ingredients---always-loser status from $2013$--$2016$ wins only and
defendant contact from $2013$--$2016$ tender-items only---and ranks
\emph{new} defendant contact arising in $2017$--$2019$.

\begin{table}[htbp]
\centering
\caption{Federal Timing and Case-Composition Synthesis}
\label{tab:federal_timing}
\scriptsize
\begin{adjustbox}{max width=\textwidth,center}
\begin{threeparttable}
\begin{tabular}{p{2.9cm}p{2.6cm}ccccccp{3.0cm}}
\toprule
Design & Information set & $N$ & Pos. & ROC-AUC & PR-AUC & Prec@500 & Rec@500 & Interpretation \\
\midrule
Full-sample retrospective & Full retrospective & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ & Construct validity; upper bound \\ % TODO_NUMBER \valFedAudRawAUC etc.
Strict full test universe & Strict; entrants at 0 & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ & Prospective collapse \\ % TODO_NUMBER \valFedAudStrictFullAUC / \valFedAudStrictPrec
Strict training AL pool & Strict; train-window AL & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ & $\valTODO$ & Incumbent-triage residue \\ % TODO_NUMBER \valFedAudStrictContAUC / \valFedAudStrictFLAUC
Label-frozen prospective & Frozen pool+score+label & $\valTODO$ & $\valTODO$ & $\valTODO$ & --- & --- & --- & New-contact ranking \\ % TODO_NUMBER \valFedAudFrozenPool / \valFedAudFrozenProspN / \valFedAudFrozenProspAUC
Label-frozen retrospective & Frozen pool+score+label & $\valTODO$ & $\valTODO$ & $\valTODO$ & --- & --- & --- & Leakage-closed retro \\ % TODO_NUMBER \valFedAudFrozenRetroAUC
\bottomrule
\end{tabular}
\begin{tablenotes}
\scriptsize
\item \textit{Notes:} In the strict and label-frozen designs the
score is frozen before the test window; later CADE adjudications are
still used as labels but were not legally observable at the
screening date. Cobidders are adjudication-anchored exposure labels,
not cartel members. The strict full universe carries entrants at
score zero; AUC uses the rank-based (Mann--Whitney) estimator under
the midrank convention. ``$0$'' denotes exactly zero true positives
in the top $500$. Every cell is a placeholder pending the federal
run chain (\valTODO).
\end{tablenotes}
\end{threeparttable}
\end{adjustbox}
\end{table}

\subsection{Case Leave-One-Case-Out (LOCO) Concentration}
\label{app:federal_loco}

\textit{Purpose.} Test whether the raw federal ranking is carried by
a single adjudicated case, mirroring the BEC leave-one-case-out
audit.

\textit{Result (pending).} The largest federal case supplies
$\valTODO$ of all positives and $\valTODO$ of the top-$500$ true % TODO_NUMBER \valFedAudTopCaseShare | \valFedAudTopCaseTP
positives. Dropping it cuts PR-AUC from $\valTODO$ to $\valTODO$ % TODO_NUMBER \valFedAudLOCOfullPRAUC | \valFedAudLOCOdropLargestPRAUC
($\valTODO$); dropping the top two cases leaves PR-AUC $\valTODO$. % TODO_NUMBER \valFedAudLOCOdropPct | \valFedAudLOCOdropTwoPRAUC
A clustered randomization-inference test (buyer$\times$year cells,
$B=1{,}000$) shows the pooled ordering is $\valTODO$ from a % TODO_NUMBER \valFedAudClusterRIverdict (distinguishable / not)
within-cell shuffle ($p=\valTODO$) while case-coverage breadth is % TODO_NUMBER \valFedAudClusterRIp
$\valTODO$ ($p=\valTODO$). The leave-one-case-out distribution is in % TODO_NUMBER \valFedAudClusterRIcovVerdict | \valFedAudClusterRIcovP
the federal supplement.

\subsection{Negative Controls}
\label{app:federal_negative_controls}

\textit{Purpose.} Establish whether any surviving federal ordering
is cartel-specific or generic co-participation geometry, mirroring
the negative-control logic of Section~\ref{sec:cobidder_type}.

\textit{Method.} The same award-layer ordering is re-evaluated
against (i) placebo anchors---random federal firms matched on
participation volume to the CADE defendants---and (ii) non-CADE
high-volume-winner anchors. If the ordering reproduces under these
placebos, it is not cartel-specific.

\textit{Result (pending).} Against placebo anchors the score retains
AUC $\valTODO$, and against non-CADE high-volume-winner anchors % TODO_NUMBER \valFedAudPlaceboAUC
$\valTODO$, versus $\valTODO$ against the CADE anchors---identifying % TODO_NUMBER \valFedAudHVWinnerAUC | \valFedAudWithinAUC
the surviving ordering as $\valTODO$. % TODO_NUMBER \valFedAudNegControlVerdict (generic geometry / cartel-specific)

\subsection{Armor Pack: Robustness of the Federal Verdict}
\label{app:federal_armor}

\textit{Purpose.} Collect the secondary federal robustness checks
that parallel the BEC armor pack: label-breadth sensitivity
($\geq 2$ shared defendant tender-items), cell-granularity stability,
and the leave-one-out exposure benchmark audit.

\textit{Result (pending).} Restricting positives to firms sharing at
least two defendant tender-items $\valTODO$ the conclusion: the % TODO_NUMBER \valFedAudBreadthVerdict (strengthens / reverses)
exposure-only model improves faster than the score, the
within-stratum AUC stays at $\valTODO$, and the nested increment is % TODO_NUMBER \valFedAudBreadthWithinAUC
$\valTODO$ ($p=\valTODO$). The firm-level leave-one-out $E_i$ % TODO_NUMBER \valFedAudBreadthIncrement | \valFedAudBreadthP
benchmark reaches $\valTODO$; recomputing each cell's density from % TODO_NUMBER \valFedAudExpLOO
non-cobidder rows only leaves a label-blind opportunity ranking at
$\valTODO$. The within-stratum AUC is stable across cell % TODO_NUMBER \valFedAudExpLB
granularities ($\valTODO$ coarse to $\valTODO$ strict). The federal % TODO_NUMBER \valFedAudWithinCoarse | \valFedAudWithinStrict
armor grids are in the supplement.

\subsection{Replication}
\label{app:federal_replication}

All federal-audit numbers are regenerable from the canonical federal
parquets under \texttt{data/processed\_comprasnet/} and the audit
outputs under \texttt{work/v22-editor/outputs/comprasnet/}; the
per-stage scripts mirror the BEC audit chain and write JSON and CSV
per stage. The cross-platform comparison feeding
Table~\ref{tab:comparative_audit} is consolidated in the federal
supplement. [SCRIPT PATHS PENDING Phase 1/2 run chain.]
