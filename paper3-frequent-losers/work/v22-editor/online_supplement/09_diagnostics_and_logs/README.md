# S-09 Diagnostics & logs

Cross-cutting scanner logs and per-script run logs supporting all sections.

**Belongs here:** the claims / number / reference / alt-text scanner CSVs,
per-script run logs, and audit-log text files.

Artifacts under `work/v22-editor/outputs/diagnostics/` (`claims_scan*.csv`,
`number_scan*.csv`, `reference_scan*.csv`, `alt_text_scan.csv`, the
`*_audit_log.txt` files) and `work/v22-editor/outputs/logs/` (`*.log`). These are
transparency / referee-response artifacts, not part of the submitted manuscript.
See `MANIFEST.csv`.
