# 07 — REPRODUCIBILITY INFRASTRUCTURE LOG (JLEO R&R v22)

Built by Subprompt 2 (2026-06-02). Infra rooted at `work/v22-editor/` (honors the "all changes in v22-editor" directive); points OUTWARD to the shared analysis pipeline.

## 1. Repo language stack
- **R 4.5.2** (dominant analysis language) — `data.table`, `fixest`, `pROC`, `DBI`/`duckdb`, `MatchIt`, `ggplot2`. New utilities are **base-R, dependency-free** so they run anywhere.
- **Python 3** — ETL builds only (`pandas`, `pyarrow`, `duckdb`).
- **LaTeX** — `elsarticle` + `natbib` (bibtex, NOT biber).
- Decision: utilities authored in **R** (the dominant analysis language). Python interop = via the shared CSV/parquet artifacts both languages read.

## 2. Manuscript build system
- No build script in the submission tree. Known compile sequence (now in `Makefile` `manuscript` target, run manually): **appendix first** (`pdflatex → bibtex → pdflatex` on `online_appendix_submission_clean`), **then paper** (`pdflatex → bibtex → pdflatex ×2` on `paper_submission_clean`) — appendix before paper so `xr` cross-refs resolve. Status: **MANUSCRIPT_BUILD_KNOWN** (documented, not auto-run).

## 3. Table/figure build system
- Tables are **inline** in the manuscript (no `\input` of table files). Figures: **2 `\includegraphics`** (`fig_data_coarsening`, `fig_temporal_holdout_roc`) from `submission_clean/output/figures/`, generated by `make_submission_figures.R` (values hardcoded) + shared scripts. Per-section table/figure builds wired in Prompts 3–11.

## 4. Known data directories
- `paper3-frequent-losers/data/processed/` — parquet + CADE CSVs (git-ignored). See `outputs/dataset_registry.csv` (13 datasets) and `00_REPO_AUDIT §C`.

## 5. Known generated-output directories
- **Shared (canonical analysis):** `paper3-frequent-losers/output/<module>/` — 40+ modules incl. `exposure_adjusted_audit`, `reverse_causality_timing`, `mechanism_within_cell`, `label_funnel`, `strict_train_threshold`; `regulatory_frontier` is EMPTY.
- **Stale:** `work/v13/output/tables/` (69 orphaned `.tex`).
- **v22 infra (NEW):** `work/v22-editor/outputs/{tables/{main,appendix},figures/{main,appendix},logs,diagnostics,cache,manuscript}` + `output_registry.csv` + `dataset_registry.csv`.
- **Directory mapping** (preferred structure → existing convention): the subprompt's `outputs/tables/`, `outputs/figures/` are created under v22-editor for revision deliverables; the *canonical analysis outputs stay at `../../output/<module>/`* and are referenced by `current_path` in `output_registry.csv`. No existing outputs moved.

## 6. Makefile targets (NEW, `work/v22-editor/Makefile`)
`diagnostics` (4 scanners + metrics test) · `audit` (regenerate registries) · `tables`/`figures` (documented placeholders) · `manuscript` (documented compile, manual) · `clean_outputs` (safe: only `outputs/cache` + `outputs/diagnostics`) · `jleo_rr_status` (branch, dirty count, planned/blocked outputs, blockers, diagnostics). No prior Makefile existed; created conservatively.

## 7. Are outputs reproducible?
- **PARTIAL, improving.** Core 01→10 reproduces via shared `00_master.R`. Revision audits 53/76/77/78/79 are RUN with artifacts + logs. **Metrics utilities have a passing 22-assertion toy test.** Registries are script-generated (no manual spreadsheet). Remaining gaps: scripts 56/80/81 not yet written/run; `cade_fl_cobidders.csv` builder absent (B3).

## 8. Known blockers from Subprompt 1 (carried)
- **B1** cobidder-definition inconsistency (193 narrow vs 341 broad) — **U2 RESOLVED: disclose + robustness; drop 30→19.**
- **B2** cobidder→case linkage — **materialized** in `output/label_funnel/case_cobidder_map.csv` (5,121 rows).
- **B3** `cade_fl_cobidders.csv` + `cade_bec_crossmatch.csv` have **no builder anywhere on disk** — JLEO replication blocker.
- **B4** conduct-onset dates absent (only judgment dates, 9/12) → rolling-origin limited to award-year; survival censored.
- **B6** `output/regulatory_frontier/` EMPTY (script 56 not run).

## 9. Immediate infrastructure risks
- `values.tex` hand-edited, not script-bound → new macros must be bound by hand with `% src:`.
- Numeric-prefix script collisions (55/61/62/67/00) — key by full filename.
- Hard-coded absolute paths in `12_build_item_value.R:42`, `00_build_bidlevel.py:40`, `67/70/73*.py` — documented, not edited (builds already produced parquets).
- 8 hard-typed numbers in manuscript (number_scan): mostly "65" (CADE rows) + the "30" conservative defendants (sec07:161, to drop→19) + app07 orphan literals.

---

## COMMANDS RUN (Subprompt 2) + OUTCOMES

| Command | Outcome |
|---|---|
| `mkdir -p outputs/... scripts/...` | ✅ created infra tree |
| `Rscript scripts/diagnostics/test_metrics_triage.R` | ✅ **22/22 assertions PASS** |
| `Rscript scripts/diagnostics/scan_claims.R` | ✅ 83 hits (0 critical, 27 high, 32 medium, 24 low/negated) |
| `Rscript scripts/diagnostics/scan_numbers.R` | ✅ 68 hits (8 hardcoded-check, 60 macro-bound) |
| `Rscript scripts/diagnostics/scan_refs.R` | ✅ 38 used / 39 defined; **0 missing**, 1 uncited (after `#1` param fix) |
| `Rscript scripts/diagnostics/scan_alt_text.R` | ✅ 2 figures, **both MISSING alt text** (compliance to-do) |
| `Rscript scripts/build/make_registries.R` | ✅ output_registry (24) + dataset_registry (13) |
| `make diagnostics` / `make audit` / `make jleo_rr_status` | ✅ all run end-to-end |

### Failures encountered + fixed
- `scan_refs.R` false-positive `#1` (LaTeX macro param in `\newcommand{\cites}[1]{\citeauthor{#1}...}`) → filtered `#`-tokens. Does NOT block.
- `make_registries.R` two rows missing `data_inputs` (10 args vs 11) → added arg-count guard reporting the offending id; fixed both. Does NOT block.
- No failure blocks later prompts.
