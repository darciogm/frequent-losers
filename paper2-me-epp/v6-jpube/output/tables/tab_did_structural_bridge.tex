\begin{table}[htbp]
\centering
\begin{threeparttable}
\caption{Bridge from DiD coefficient to structural counterfactual}
\label{tab:did_structural_bridge}
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{lrr}
\toprule
Source of divergence & Non-pharma & Pharma \\
\midrule
DiD coefficient on $\log p^{\mathrm{final}}$ (Table~\ref{tab:prices}, 18m, with PBU) & $-0.113$ & $-0.113$ \\
\quad converted to absolute price change ($e^{0.113} - 1$) & $+0.120$ & $+0.120$ \\
\midrule
\textit{Sources of structural-DiD divergence:} & & \\
\quad (a) Sample restriction: Preg\~ao only, 18m, $c_\epsilon \le \filterCepsUpper$, $n \ge \filterMinBidders$ & $\bridgeSampleNp$ & $\bridgeSamplePh$ \\
\quad (b) UH cleaning: Krasnokutskaya scale shrinkage of $a_t$ & $\bridgeUhNp$ & $\bridgeUhPh$ \\
\quad (c) Functional form: second-order statistic vs.\ linear log-mean & $\bridgeFuncNp$ & $\bridgeFuncPh$ \\
\quad (d) Conditioning: counterfactual entry profile vs.\ realized entry & $\bridgeCondNp$ & $\bridgeCondPh$ \\
\quad Sub-total: DiD baseline plus four contributions, $p^{\mathrm{ref}}$ normalization & $\bridgeSumNp$ & $\bridgeSumPh$ \\
\midrule
Structural $p_{S_3} - p_{S_1}$ (Table~\ref{tab:v3_bne_decomp}) & $+\bneEffectTotalNp$ & $+\bneEffectTotalPh$ \\
Residual (structural minus sub-total) & $\bridgeResidNp$ & $\bridgeResidPh$ \\
\bottomrule
\end{tabular}
\begin{tablenotes}\footnotesize
\item Decomposition of the gap between the DiD coefficient (\ref{app:did})
and the structural counterfactual (Section~\ref{sec:results}). The DiD on
$\log p^{\mathrm{final}}$ is first converted to an absolute price-change scale
($e^{|\hat\beta|} - 1$). Four mechanical sources account for the remaining
divergence: (a) the structural sample restricts to Preg\~ao auctions with
$c_\epsilon \le 3$ and $n \ge 2$, dropping observations where the DiD averages
include marginal-bidder noise; (b) UH cleaning shrinks the auction-level scale
$a_t$ out of each log-bid, exposing the right tail of the bidder-specific
component; (c) the second-order statistic of an SME-only IPV draw is a
non-linear functional of $F_c^k$, larger than the linear log-mean recovered
by the DiD; (d) the structural counterfactual integrates against the
counterfactual entry profile (Pre-period non-SMEs removed, Post-period SME
pool), while the DiD averages over the realized entry distribution. The
sub-totals are informal accounting estimates (median per-cell contribution
from auxiliary regressions and decomposition arithmetic), not the output of a
formal Oaxaca-Blinder-style identified decomposition; they are reported here
to demonstrate that the magnitude of the structural-DiD gap is mechanically
attributable to the four sources, not to characterize the contribution of each
source with statistical precision. The pharma residual ($+0.004$, within
Monte Carlo noise) confirms tight attribution; the non-pharma residual
($-0.028$) indicates the four mechanical sources slightly over-explain the
gap, so the bridge accounting should be read as an attribution exercise
rather than as an identified decomposition. The 2--3$\times$ structural-DiD
ratio (structural $+\bneEffectTotalNp$/$+\bneEffectTotalPh$ vs.\ DiD
absolute-scale baseline $+0.120$) is therefore mechanically attributable
to the four sources above and is predictable in direction (structural $>$
DiD) under correct specification of the structural model relative to the
DiD reduced form.
\end{tablenotes}
\end{threeparttable}
\end{table}
